import {localeString, localeText, localeBlock} from './localeTypes'
import event from './event'
import session from './session'
import galleryImage from './galleryImage'
import galleryGroup from './galleryGroup'

export const schemaTypes = [localeString, localeText, localeBlock, event, session, galleryGroup, galleryImage]
